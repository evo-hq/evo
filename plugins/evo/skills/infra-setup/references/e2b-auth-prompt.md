To use the `e2b` provider, first make sure the E2B SDK is installed into the same environment as `evo`, then export your API key:

```bash
export E2B_API_KEY=...
```

Get the key from the E2B dashboard team/settings area, then rerun the command above in the same shell that launches `evo`.
